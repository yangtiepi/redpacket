<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liuhe.redpacket.mapper.AdMapper" >
  <insert id="save" parameterType="Ad" useGeneratedKeys="true" keyProperty="id">
    insert into t_ad (
        	title ,
        	smallImg ,
        	image ,
        	content ,
        	info ,
        	url ,
        	sort ,
        	clickedNum ,
        	clickableNum ,
        	periodDay ,
        	beginTime ,
        	endTime ,
        	createTime ,
        	status ,
        	isDel ,
        	delTime ,
        	qrcode ,
        	homepageId ,
        	redpacketId
        ) VALUES (
        	#{title} ,
        	#{smallImg} ,
        	#{image} ,
        	#{content} ,
        	#{info} ,
        	#{url} ,
        	#{sort} ,
        	#{clickedNum} ,
        	#{clickableNum} ,
        	#{periodDay} ,
        	#{beginTime} ,
        	#{endTime} ,
        	#{createTime} ,
        	#{status} ,
        	#{isDel} ,
        	#{delTime} ,
        	#{qrcode} ,
        	#{homepageId} ,
        	#{redpacketId}
        )
  </insert>
  
  <resultMap type="Ad" id="AdMap">
		<id property="id" column="id"/>
		<result property="title" column="title"/>
		<result property="smallImg" column="smallImg"/>
		<result property="image" column="image"/>
		<result property="content" column="content"/>
		<result property="info" column="info"/>
		<result property="url" column="url"/>
		<result property="sort" column="sort"/>
		<result property="clickedNum" column="clickedNum"/>
		<result property="clickableNum" column="clickableNum"/>
		<result property="periodDay" column="periodDay"/>
		<result property="beginTime" column="beginTime"/>
		<result property="endTime" column="endTime"/>
		<result property="createTime" column="createTime"/>
		<result property="status" column="status"/>
		<result property="isDel" column="isDel"/>
		<result property="delTime" column="delTime"/>
		<result property="qrcode" column="qrcode"/>
		<association property="redpacket" column="redpacketId" select="com.liuhe.redpacket.mapper.RedpacketMapper.get"/> 
		<collection property="homepages" column="id" select="com.liuhe.redpacket.mapper.HomepageMapper.getByAd"/>
	</resultMap>
  
   <update id="update" parameterType="Ad">
  	update t_ad set 
  		title = #{title} ,
	        <if test="smallImg != null">
        		smallImg = #{smallImg} ,
  			</if>
	        <if test="image != null">
        		image = #{image} ,
  			</if>
	        <if test="clickedNum != null">
        		clickedNum = #{clickedNum} ,
  			</if>
	        <if test="beginTime != null">
        		beginTime = #{beginTime} ,
  			</if>
	        <if test="endTime != null">
        		endTime = #{endTime} ,
  			</if>
	        <if test="status != null">
        		status = #{status} ,
  			</if>
	        <if test="isDel != null">
        		isDel = #{isDel} ,
  			</if>
	        <if test="delTime != null">
        		delTime = #{delTime} ,
  			</if>
	        <if test="qrcode != null">
        		qrcode = #{qrcode} ,
  			</if>
	        content = #{content} ,
	        info = #{info} ,
	        sort = #{sort} ,
	        periodDay = #{periodDay} ,
	        homepageId = #{homepageId} ,
	        clickableNum = #{clickableNum} ,
	        url = #{url} ,
	        redpacketId = #{redpacketId} 
  	where id=#{id} 
  </update>
  
  <delete id="delete" parameterType="long" >
    delete from t_ad
    where id = #{id}
  </delete>
  
  <select id="get" resultType="Ad" parameterType="long">
   	 select * from t_ad 
   	 where id = #{id}
  </select>
  <select id="getByRedpacket" resultType="Ad" parameterType="long">
   	 select * from t_ad 
   	 where redpacketId = #{redpacketId}
  </select>
  
  <select id="getAll" resultType="Ad" >
   	 select * from t_ad where isDel =0
  </select>
  <select id="listNoUse" resultType="Ad" >
   	 select * from t_ad  
   	 where isDel =0 and id not in (select DISTINCT adId from t_ad_homepage) 
  </select>
  
  <sql id="whereSql">
		<where>
			<if test="status != null and status != -1">
				and a.status = #{status}
			</if>
			<if test="isDel != null">
				and a.isDel = #{isDel}
			</if>
			<if test="isDel == null">
				and a.isDel = 0
			</if>
			<if test="homepageId != null and homepageId != -1">
				and ah.homepageId = #{homepageId}
			</if>
			<if test="title != null and title != ''">
				and a.title = #{title}
			</if>
			<if test="isValid != null and isValid == 1">
				and a.clickableNum > a.clickedNum 
			</if>

		</where>
	</sql>
  
  <select id="query" resultMap="AdMap"  parameterType="BaseQuery">
		select a.*
		from t_ad a
		left join t_ad_homepage ah on a.id = ah.adId
		<include refid="whereSql" />
		group by a.id
		order by a.sort
		<if test="pageSize!=-1">
			limit #{start},#{pageSize}
		</if>
	</select>
	<!-- 获取总记录数 -->
	<select id="queryTotal" resultType="long" parameterType="BaseQuery">
		select count(1) from
		(select count(1)
		from t_ad a 
		left join t_ad_homepage ah on a.id = ah.adId
		<include refid="whereSql" />
		group by a.id)c
	</select>
	
  <select id="queryByUser" resultMap="AdMap"  parameterType="BaseQuery">
		select a.id,
			a.title ,
        	a.smallImg ,
        	a.image ,
        	a.content ,
        	a.info ,
        	a.url ,
        	a.sort ,
        	a.clickableNum ,
        	a.periodDay ,
        	a.beginTime ,
        	a.endTime ,
        	a.createTime ,
        	a.status ,
        	a.isDel ,
        	a.delTime ,
        	a.qrcode ,
        	a.homepageId ,
        	a.redpacketId,
		(select count(1) from t_ad_click_log where openid = #{openid} and adId = a.id and DATEDIFF(ac.clickTime,now()) &lt; a.periodDay) clickedNum
		from t_ad a
		left join t_ad_homepage ah on a.id = ah.adId
		left join t_ad_click_log ac on a.id = ac.adId
		where ah.homepageId = #{homepageId}  
		and (select count(1) from t_ad_click_log where openid = #{openid} and adId = a.id) &lt; a.clickableNum
		group by a.id
		order by clickedNum desc,a.sort
		<if test="pageSize!=-1">
			limit #{start},#{pageSize}
		</if>
	</select>
	<!-- 获取总记录数 -->
	<select id="queryByUserTotal" resultType="long" parameterType="BaseQuery">
		select count(1) from
		(select a.*
		from t_ad a
		left join t_ad_homepage ah on a.id = ah.adId
		left join t_ad_click_log ac on a.id = ac.adId
		where ah.homepageId = #{homepageId}  
		and (select count(1) from t_ad_click_log where openid = #{openid} and adId = a.id) &lt; a.clickableNum
		group by a.id
		order by a.sort
		)c
	</select>
</mapper>